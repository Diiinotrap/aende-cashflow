services:
  - type: web
    name: aende-cashflow
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GOOGLE_SHEETS_WEBHOOK_URL
        sync: false
